<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hakku.main.community.repository.PostRepository">

    <sql id="columns">
        id, author_id, title, content, board, image_url, created_at, updated_at
    </sql>

    <insert id="insert" parameterType="com.hakku.main.community.domain.Post"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO posts (author_id, title, content, board, image_url, created_at, updated_at)
        VALUES (#{authorId}, #{title}, #{content}, #{board}, #{imageUrl}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="com.hakku.main.community.domain.Post">
        UPDATE posts
        SET title = #{title},
            content = #{content},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM posts WHERE id = #{id}
    </delete>

    <select id="selectById" resultType="com.hakku.main.community.domain.Post">
        SELECT <include refid="columns"/>
        FROM posts
        WHERE id = #{id}
    </select>

    <select id="findAllByOrderByIdDesc" resultType="com.hakku.main.community.domain.Post">
        SELECT <include refid="columns"/>
        FROM posts
        ORDER BY id DESC
    </select>

    <select id="findAllByBoardOrderByIdDesc" resultType="com.hakku.main.community.domain.Post">
        SELECT <include refid="columns"/>
        FROM posts
        WHERE board = #{board}
        ORDER BY id DESC
    </select>

    <select id="findAllByAuthorIdOrderByIdDesc" resultType="com.hakku.main.community.domain.Post">
        SELECT <include refid="columns"/>
        FROM posts
        WHERE author_id = #{authorId}
        ORDER BY id DESC
    </select>
</mapper>
