<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hakku.main.community.repository.CommentRepository">

    <insert id="insert" parameterType="com.hakku.main.community.domain.Comment"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comments (post_id, author_id, content, created_at, updated_at)
        VALUES (#{postId}, #{authorId}, #{content}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="com.hakku.main.community.domain.Comment">
        UPDATE comments
        SET content = #{content},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM comments WHERE id = #{id}
    </delete>

    <select id="selectById" resultType="com.hakku.main.community.domain.Comment">
        SELECT id, post_id, author_id, content, created_at, updated_at
        FROM comments
        WHERE id = #{id}
    </select>

    <select id="findByPostIdOrderByIdAsc"
            resultType="com.hakku.main.community.domain.Comment">
        SELECT id, post_id, author_id, content, created_at, updated_at
        FROM comments
        WHERE post_id = #{postId}
        ORDER BY id ASC
    </select>

    <select id="countByPostId" resultType="long">
        SELECT COUNT(*) FROM comments WHERE post_id = #{postId}
    </select>
</mapper>
