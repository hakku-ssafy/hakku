<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hakku.main.community.repository.PostLikeRepository">

    <insert id="insert" parameterType="com.hakku.main.community.domain.PostLike"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO post_likes (post_id, user_id, created_at)
        VALUES (#{postId}, #{userId}, #{createdAt})
    </insert>

    <select id="existsByPostIdAndUserId" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM post_likes
            WHERE post_id = #{postId} AND user_id = #{userId}
        )
    </select>

    <delete id="deleteByPostIdAndUserId">
        DELETE FROM post_likes
        WHERE post_id = #{postId} AND user_id = #{userId}
    </delete>

    <select id="countByPostId" resultType="long">
        SELECT COUNT(*) FROM post_likes WHERE post_id = #{postId}
    </select>
</mapper>
